# Solo necesario para ejecutar alembic manualmente fuera de Docker
# En producción las variables las inyecta docker-compose.yml
# Genera SECRET_KEY con: python -c "import secrets; print(secrets.token_hex(32))"
DATABASE_URL=postgresql+asyncpg://seo_user:yourpassword@localhost:5432/suite_seo
SECRET_KEY=cambia-esto-minimo-32-caracteres-aleatorios
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
CORS_ORIGINS=https://seo.prensaiberica.es
